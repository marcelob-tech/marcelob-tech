name: Generate Snake 🐍

on:
  schedule:
    - cron: "0 0 * * *" # executa 1x por dia (meia-noite UTC)
  workflow_dispatch: # permite rodar manualmente
  push:
    branches:
      - master

jobs:
  build:
    name: Geração da cobrinha 🐍
    runs-on: ubuntu-latest

    steps:
      - name: 🔍 Checkout do repositório
        uses: actions/checkout@v3

      - name: 🐍 Gerar animação da cobrinha
        uses: Platane/snk@v3
        with:
          github_user_name: marcelob-tech
          outputs: |
            dist/github-contribution-grid-snake.svg
            dist/github-contribution-grid-snake-dark.svg?palette=github-dark
            dist/github-contribution-grid-snake-neon.svg?color_snake=#00ffcc&color_dots=#a020f0,#40e0d0,#00ffff,#adff2f,#39ff14

      - name: 🚀 Publicar no branch "output"
        uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
